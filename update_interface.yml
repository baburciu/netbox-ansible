# ======= How to run standalone:
# boburciu@WX-5CG020BDT2: ~$ cd ~/netbox-ansible-automation
# boburciu@WX-5CG020BDT2:~/netbox-ansible-automation$ ansible-playbook -i ./hosts -v update_interface.yml -e 'lag_name="Eth-Trunk3" interface_name="10GE1/0/1" interface_device="SWH-OoB-R2" external_vars="./external_vars.yml" ansible_python_interpreter="/usr/bin/python3" url_var="http://192.168.200.23:8001/" token_var="aca25c1c8ce2c6f205b407cb7352c68cc69cc58b"'

- name: Update interface
  connection: local
  hosts: docker_netbox_19216820023
  gather_facts: False
  collections:
    - netbox.netbox 
  tasks:
    # # - include_vars: external_vars.yml  
    # - include_vars: '{{external_vars}}'    

    - name: Update interface with LAG and VLAN
      netbox_device_interface:
        netbox_url: '{{ url_var }}'
        netbox_token: '{{ token_var }}'
        data:
          device: '{{ interface_device }}'
          name: '{{ interface_name }}'
          lag:
            name: '{{ lag_name }}'
          mode: '{{ dot1q_mode }}'
          untagged_vlan:
            - name: '{{ untagged_vlan_id }}'
          tagged_vlans:
            - name: '{{ tagged_vlan_id }}'                      
        state: present    
